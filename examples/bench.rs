extern crate pubsub;

use pubsub::server::Server; 
use pubsub::server::Topics;

fn main() {
    // match Server::bind("localhost:5000", Topics::new()) {
    //     Ok(_)  => println!("server ended ok"),
    //     Err(e) => println!("{:?}", e)
    // };  
}